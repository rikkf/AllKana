<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.codanimex.android.allkana">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".tabs.HiraganaTab"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".tabs.KatakanaTab"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".HiraganaViewActivity" />
        <activity android:name=".KatakanaViewActivity" />
        <activity android:name=".HiraganaLearn" />
        <activity android:name=".KatakanaLearn" />
        <activity android:name=".quiz.HiraganaQuiz"></activity>
    </application>

</manifest>